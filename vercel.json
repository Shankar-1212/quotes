{
    "builds": [{
        "src": "quotes_api/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "quotes_api/wsgi.py"
        }
    ]
}
